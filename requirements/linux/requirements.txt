# The linux / production lock: what `pip install .[quantum]` actually resolved
# inside the image, frozen so a rebuild installs the same stack twice running.
#
# This is NOT the developer environment. requirements.txt is the Intel-Mac parity
# file and is capped at torch 2.2.2 (PyTorch's last x86_64-macOS wheel), which in
# turn caps numpy below 2 and pennylane below 0.45. Here, none of that applies:
# numpy 2, pennylane 0.45 and the current qiskit are what production runs.
#
# torch and torchvision are not in this file — they install from PyTorch's CPU
# index, pinned in torch.txt, and move by hand as a pair.
#
# Regenerate: docker build -t qml-lock . && docker run --rm qml-lock pip freeze
appdirs==1.4.4
autograd==1.9.1
autoray==0.8.4
blinker==1.9.0
cachetools==7.2.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
cloudpickle==3.1.2
diastatic-malt==2.15.3
dill==0.4.1
filelock==3.32.6
Flask==3.1.3
flask-cors==6.0.5
fsspec==2026.7.0
gast==0.7.0
gunicorn==26.2.0
idna==3.20
itsdangerous==2.2.0
Jinja2==3.1.6
joblib==1.6.0
MarkupSafe==3.0.3
mistune==3.3.4
mpmath==1.3.0
narwhals==2.26.0
networkx==3.6.1
numpy==2.5.3
packaging==26.3
pennylane==0.45.1
pennylane_lightning==0.45.0
pillow==12.3.0
psutil==7.2.2
python-dateutil==2.9.0.post0
qiskit==2.5.2
qiskit-aer==0.17.2
requests==2.34.2
rustworkx==0.18.1
scikit-learn==1.9.1
scipy==1.18.1
scipy-openblas32==0.3.34.237.0
setuptools==84.0.0
six==1.17.0
stevedore==5.9.1
sympy==1.14.0
termcolor==3.3.0
threadpoolctl==3.7.0
tomlkit==0.15.1
typing_extensions==4.16.0
urllib3==2.8.0
Werkzeug==3.1.8
